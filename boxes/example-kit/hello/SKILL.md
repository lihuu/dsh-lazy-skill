---
name: hello
description: "The first sub-skill of example-kit. Demonstrates a lazily loaded sub-skill body."
---

# hello（example-kit 子 skill）

这是一个**示例子 skill** 的正文。当你加载 `example-kit`（配置了 `loadSubskills`）
时，本子 skill 的正文会被自动带进来。把这里替换成你真正想要的指令即可。
